---
tags: [index, outils, cli]
---

# Outils — Map of Content

## Runtime / Profiling

- [[clr-profiler]] — CLR Profiler API .NET : injection d'agent dans un processus via variables d'env, sans IPC
- [[java-agent]] — Java Agent (JVM Instrumentation API) : réécriture bytecode à chaud via -javaagent

## Données / CLI

- [[yq]] — Processeur YAML en ligne de commande, équivalent de `jq` pour le YAML

## Réseau / Sécurité

- [[openssl]] — Inspecter handshakes TLS, certificats et chaînes en CLI (voir [[_index-tls|index TLS]])
- [[diagnostic-par-couche]] — `dig`, `traceroute`, `ss`, `tcpdump`, `curl -v` : un outil par couche réseau
