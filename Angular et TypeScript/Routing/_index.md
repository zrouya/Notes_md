---
tags: [moc, angular, typescript]
---

# Routing

Notes sur le système de routage Angular : configuration des routes, module Router, et affichage via `router-outlet`.

## Notes

- [[Routing Angular]] — vue d'ensemble du système de routage Angular
- [[Module Router Angular]] — le `RouterModule` fournissant les services de routage
- [[Configuration des routes en Angular]] — configuration des routes de l'application
- [[Routes Angular]] — l'objet `Routes`, tableau de configurations de routes
- [[Routes imbriquées en Angular]] — arborescence de routes imbriquées
- [[RouterOutlet Angular]] — point d'ancrage d'affichage du composant routé
- [[RouterOutlet nommés]] — outlets multiples pour des vues secondaires
