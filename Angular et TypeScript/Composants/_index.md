---
tags: [moc, angular, typescript]
---

# Composants

Notes sur les composants Angular : leur structure, leurs métadonnées, la communication parent/enfant, les directives structurelles et les signals.

## Notes

- [[Composants Angular]] — structure de base d'un composant (classe, template, style)
- [[Métadonnées d'un composant Angular]] — le décorateur `@Component` et ses options
- [[Angular Component Inputs-Outputs]] — communication parent/enfant via `@Input`/`@Output`
- [[Composants Angular - Data Binding]] — moyens de lier données et template dans un composant
- [[Angular Content Projection]] — projection de contenu via `ng-content`
- [[Directives Angular]] — directives de composant, structurelles et d'attribut
- [[Angular Pipes]] — formatage des property bindings via les pipes
- [[Angular ngFor]] — génération de listes de composants avec `NgFor`
- [[Angular ngIf]] — rendu conditionnel avec `NgIf`
- [[Angular Signals]] — gestion d'état réactive par signals (depuis Angular 16)
