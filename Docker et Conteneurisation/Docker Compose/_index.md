---
tags: [moc, docker]
---

# Docker Compose

Définition, build et exécution d'applications multi-conteneurs via des fichiers docker-compose.

## Notes

- [[Docker Compose]] — outil de gestion du build/déploiement/exécution d'ensembles de conteneurs
- [[Docker Compose CLI]] — outil CLI intégré à Docker pour piloter Compose
- [[Fichier Docker compose]] — structure du fichier docker-compose.yaml
- [[Docker Compose build]] — paramètres de build d'image dans un service Compose
- [[Extensions de Docker Compose files]] — extension de fichiers Compose (`extends`) pour gérer le cycle de vie applicatif
